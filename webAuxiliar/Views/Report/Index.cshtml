@{
    ViewBag.Title = "Reporte";
}
<!DOCTYPE html>
<html lang="es-ec">
<head>
    <style>
        h3 {
            background-color: powderblue;
        }
    </style>
    @*<link type="text/css" rel="stylesheet" href="~/Scripts/waitMe/waitMe.css">*@
    <link type="text/css" rel="stylesheet" href="~/Scripts/jqLoading/loading.css">
    <script src="~/Scripts/jquery-3.2.1.min.js"></script>
    <script src="~/Scripts/jqLoading/loading.js"></script>
</head>
<body>
    <div id="container">
        <form>
            <div class="row">
                <div class="col-md-4">
                    <h3 style="color:black">AUXILIAR OBRA</h3>
                </div>
                <div class="col-md-4">
                    <h3 style="color:black">AUXILIAR SERVICIO</h3>
                </div>
            </div>
            <hr />
            <div class="row">
                <div class="col-md-2">
                    <input type="text" class="form-control" id="rptAuxObra" placeholder="Enter Id AuxObra">
                </div>
                <div class="col-md-2">
                    <button type="button" id="btnSearchObra" class="btn btn-raised btn-primary btn-block">Generar</button>
                </div>
                <div class="col-md-2">
                    <input type="text" class="form-control" id="rptAuxServicio" placeholder="Enter Id AuxServicio">
                </div>
                <div class="col-md-2">
                    <button type="button" id="btnSearchServ" class="btn btn-raised btn-primary btn-block">Generar</button>
                </div>
            </div>

        </form>

        <div class="row" id="divReport">
        </div>
    </div>


</body>
</html>

@section scripts
{
    @*<script src="~/Scripts/jquery-2.1.1.min.js"></script>*@
    @*<script src="~/Scripts/jquery-3.2.1.min.js"></script>*@
    @*<script src="~/Scripts/waitMe/waitMe.js"></script>*@
    @*<script>
        $(function () {
            var current_effect = 'roundBounce';

            $('#btnSearchObra').click(function () {
                run_waitMe(current_effect);
            });


            function run_waitMe(effect) {
                text = 'Please wait...';
                fontSize = '';
                $('#btnSearchObra').waitMe(
                {
                    effect: effect,
                    text: '',
                    bg: 'rgba(255,255,255,0.7)',
                    color: '#000',
                    //maxSize: maxSize,
                    waitTime: -1,
                    //source: 'img.svg',
                    //textPos: textPos,
                    //fontSize: '',
                    onClose: function () { }
                })
            }

            $('#divReport').waitMe('hide');

        });
    </script>*@
    <script>
        $(function ()
        {
            $('#btnSearchObra').click(function ()
            {
                //$('body').loading(
                //{
                //    stoppable: true,
                //    message: 'Cargando reporte. Por favor espere...',
                //    theme: 'dark'
                //});
                var rptAuxObra = $("#rptAuxObra").val();
                //ASPX page URL to load report
                var src = '../Reportes/rptViewer.aspx?';
                //We can add parameters here
                src = src + "rptAuxObra=" + rptAuxObra

                //We can append more than one parameter like below
                //var companyId = 1
                //src = src + "compnayID=" + companyId

                //Create a dynamic iframe here and append this to div tag
                var iframe = '<iframe id="myReportFrame" width="100%" height="800px" scrolling="no" frameborder="0" src="' + src + '" allowfullscreen></iframe>';
                $("#divReport").html(iframe);
                
                //var current_effect = 'roundBounce';
                //run_waitMe(current_effect);
                //function run_waitMe(effect)
                //{
                //    text = 'Please wait...';
                //    fontSize = '';
                //    $('#container').waitMe(
                //    {
                //        effect: effect,
                //        text: '',
                //        bg: 'rgba(255,255,255,0.7)',
                //        color: '#000',
                //        //maxSize: maxSize,
                //        waitTime: -1,
                //        //source: 'img.svg',
                //        //textPos: textPos,
                //        //fontSize: '',
                //        onClose: function () { }
                //    })
                //}

                //$('#container').click(function ()
                //{
                //    $(this).waitMe('hide');
                //});
                

                
            });
        });

    </script>
    <script>
        $(function () {
            $('#btnSearchServ').click(function () {
                //$('#btnSearchObra').waitMe('hide');
                var rptAuxServicio = $("#rptAuxServicio").val();

                //ASPX page URL to load report
                var src = '../Reportes/rptViewer.aspx?';
                //We can add parameters here
                src = src + "rptAuxServicio=" + rptAuxServicio

                //We can append more than one parameter like below
                //var companyId = 1
                //src = src + "compnayID=" + companyId

                //Create a dynamic iframe here and append this to div tag
                var iframe = '<iframe id="myReportFrame" width="100%" height="800px" scrolling="no" frameborder="0" src="' + src + '" allowfullscreen></iframe>';
                $("#divReport").html(iframe);
            });
        });

    </script>

}